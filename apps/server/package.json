{
  "name": "server",
  "main": "./dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./dist/index.js --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.js",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix"
  },
  "dependencies": {
    "hono": "catalog:",
    "@trpc/server": "catalog:",
    "@hono/trpc-server": "0.4.0",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "zod": "catalog:",
    "@packages/api": "workspace:*",
    "@packages/auth": "workspace:*",
    "@packages/db": "workspace:*"
  },
  "devDependencies": {
    "@packages/eslint-config": "workspace:*",
    "@types/bun": "1.2.6",
    "tsdown": "catalog:",
    "typescript": "catalog:"
  }
}
